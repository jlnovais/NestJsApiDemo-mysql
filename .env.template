PORT=3210

SESSION_SECRET=123456789abc

#mySql settings
#DB_HOST=localhost
#DB_PORT=3306
#DB_USERNAME=demoNest
#DB_PASSWORD=123
#DB_DATABASE=demo-nest

#mySql settings - using proxySql - port 6033
DB_HOST=localhost
DB_PORT=6033
DB_USERNAME=root
DB_PASSWORD=123
DB_DATABASE=demo-nest


# Email/SMTP Configuration
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

#environment: "production" or anything else for develoment; in production sends the cookie only over HTTPS; in development allows HTTP
NODE_ENV=develoment

# session login details
CODE_EXPIRY_MINUTES=5
SESSION_EXPIRY_MINUTES=60
SESSION_SECRET=secret_muito_secreto!%123456

# Redis Configuration
REDIS_ENABLED=true                    # Set to 'true' to enable Redis, 'false' to use in-memory storage
REDIS_HOST=localhost                  # Redis host (default: localhost)
REDIS_PORT=6379                       # Redis port (default: 6379)
REDIS_PASSWORD=123                    # Redis password (optional, leave empty if no password)
REDIS_DB=0                            # Redis database number (default: 0)

#storage (oracle cloud - compatible with amazon S3)

OCI_REGION=eu-amsterdam-1
OCI_NAMESPACE=my_namespace...
OCI_BUCKET_NAME=bucket-demo...
OCI_ACCESS_KEY_ID=my_access_key_id....
OCI_SECRET_ACCESS_KEY=my_secret_access_key...

# S3-Compatible Endpoint (for OCI) - optional; if not provided, the endpoint URL will be created with the above parameters
#OCI_S3_ENDPOINT=https://my_namespace.compat.objectstorage.eu-amsterdam-1.oraclecloud.com

#-----------------------------------------------------------------
# RabbitMQ Sender Configuration

RABBITMQ_EMPLOYEE_EVENTS_QUEUE_OR_ROUTINGKEY=employee.events # leave empty for turn off the sending of messages
RABBITMQ_USER_QUEUE_OR_ROUTINGKEY=testeQueue

RABBITMQ_EXCHANGE_NAME_SENDER=
RABBITMQ_HOST_SENDER="127.0.0.1" # or use multiple hosts separated by ',' or ';'
RABBITMQ_PORT_SENDER=5672
RABBITMQ_VHOST_SENDER=myvhost
RABBITMQ_USER_SENDER=username
RABBITMQ_PASSWORD_SENDER=pass
RABBITMQ_CONNECTION_DESCRIPTION_SENDER=MyApp RabbitMQ Connection... Testing...

RABBITMQ_SELECT_RANDOM_HOST=false # true / false
RABBITMQ_SELECT_SEQUENCIAL_HOST=true # true / false
RABBITMQ_CONNECTION_TIMEOUT=7000 # milliseconds
RABBITMQ_CONNECTION_RETRY_DELAY=2000 # in milliseconds; 0 means no retry, the same as RABBITMQ_CONNECTION_RETRY_ATTEMPTS=0
RABBITMQ_CONNECTION_RETRY_ATTEMPTS=10 # -1 means no limit (will retry indefinitely); 0 means no retries

#---------------------------------------------------------------
#consumer configuration
RABBITMQ_USER_QUEUE_CONSUMER=testeQueue

RABBITMQ_HOST_CONSUMER="127.0.0.1" # use multiple hosts separated by ',' or ';' | only sequential connection is supported by consumers - try each host in turn sequentially 
RABBITMQ_PORT_CONSUMER=5672
RABBITMQ_VHOST_CONSUMER=nmyvhost
RABBITMQ_USER_CONSUMER=username
RABBITMQ_PASSWORD_CONSUMER=pass

RABBITMQ_CONSUMER_INSTANCES_TO_START=2 #total consumer instances to start

RABBITMQ_CONNECTION_DESCRIPTION_CONSUMER=MyApp connection (consumer)...

RABBITMQ_CONNECTION_TIMEOUT_CONSUMER=7000 # milliseconds
RABBITMQ_MAX_CHANNELS_PER_CONNECTION=3

RABBITMQ_RETRY_QUEUE=retryQueue # Optional retry queue name for delayed processing
RABBITMQ_RETRY_QUEUE_MESSAGE_TTL_IN_SECONDS_MIN=30 # Optional message TTL in seconds for retry queue; if MIN and MAX are 0, that means no TTL (messages will not expire)
RABBITMQ_RETRY_QUEUE_MESSAGE_TTL_IN_SECONDS_MAX=0  # Optional message TTL in seconds for retry queue; if MIN and MAX are 0, that means no TTL (messages will not expire)

RABBITMQ_CONSUMER_MAX_RECONNECT_ATTEMPTS=9 # reconnect if connection is closed; 0: disabled (no retry); -1: infinite retries; >0 max retry attempts

RABBITMQ_CONSUMER_ENABLED=true #start consumers